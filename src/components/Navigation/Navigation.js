import React from "react";
import "tachyons";

const Navigation = () => {
  return (
    <nav style={{ display: "flex", justifyContent: "flex-end" }}>
      <p>Logout</p>
    </nav>
  );
};

export default Navigation;
